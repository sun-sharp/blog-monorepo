export interface PaginationProps {
  page?: number;
  pageCount?: number;
  pageSize?: number;
  itemCount?: number;
  pageSizes?: number[];
  showSizePicker?: boolean;
  showQuickJumper?: boolean;
}
