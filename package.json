{
  "name": "blog-monorepo",
  "private": true,
  "version": "4.4.0",
  "workspaces": [
    "packages/website",
    "packages/admin-web",
    "packages/node-server",
    "packages/shared"
  ],
  "scripts": {
    "typecheck:shared": "npm run typecheck --workspace=packages/shared",
    "git:dev-main-dev": "git push origin dev && git checkout main && git pull origin dev && git push origin main && git checkout dev"
  }
}